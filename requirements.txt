# requirements.txt

# Django-фреймворк для реализации бизнес-логики и интерфейса администратора
Django==4.2.7

# Библиотека для взаимодействия с БД SQLite (входит в стандартную библиотеку Python)
# но оставим sqlite-utils для удобства работы с БД вручную
sqlite-utils==3.35

# Для паролей, безопасности и шифрования
bcrypt==4.1.2
python-dotenv==1.0.1

# Для валидации данных и работы с API
djangorestframework==3.14.0

# Для красивых HTML-форм (если нужно)
django-crispy-forms==2.1

# Для работы с временными метками
pytz==2024.1

# Для генерации отчетов (если потребуется PDF/Excel)
reportlab==4.1.0
openpyxl==3.1.2

# Для загрузки файлов, если реализуется хранение файлов разработчиков
Pillow==10.2.0

# Проверка безопасности и уязвимостей (статический анализ)
bandit==1.7.6
